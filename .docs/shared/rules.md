# Shared Rules

## Purpose

Record project-wide rules that are stable enough to affect multiple tasks or code areas.

## Rules

- Code is the source of truth for runtime behavior.
- Small tasks should stay in `.docs/work/**` unless a lens justifies heavier structure.
- Long-term facts must keep a source link.
